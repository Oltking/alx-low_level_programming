#include "main.h"

/**
 * set_string - function to set pointer to a string
 *
 * @s: param for pointer
 * @to: param for pointed value
 *
 * Return: return 0 if successful
 */

void set_string(char **s, char *to)
{
	*s = to;
}
